import_trello.php
import-external-repo.sh
# Si on met vendor, on ignore tout fichier ou répertoire s'appelant
# vendor, quel que soit son emplacement dans le projet.
# Si on met /vendor, on ignore uniquement le fichier ou répertoire
# se trouvant à la racine du dépôt
/vendor
config.ini
